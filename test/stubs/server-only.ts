// Stub vazio para o pacote `server-only` (exports condicionais do Next.js que o vitest não resolve).
// Em runtime real o import só marca o módulo como server-side; nos testes é um no-op.
export {};
