{
  "name": "@template/worker-track",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker de tracking server-side (Onda 10) — POST /e, espelho NO-PII em lp_events, fan-out CAPI/GA4.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:apply": "wrangler d1 execute track --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.99.0"
  }
}
